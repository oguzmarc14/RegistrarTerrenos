---
import Layout from "../layouts/Layout.astro";
import { createClient } from "@supabase/supabase-js";

const supabase = createClient(
  import.meta.env.PUBLIC_SUPABASE_URL,
  import.meta.env.PUBLIC_SUPABASE_KEY
);

const { data: terrenos, error } = await supabase
  .from("Terrenos")
  .select("*")
  .order("fecha", { ascending: false });
---

<Layout>
  <h1 class="text-3xl font-bold text-center mb-10">Terrenos registrados</h1>

  {error && <p class="text-red-500 text-center">Error al cargar terrenos</p>}

  <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3 max-w-6xl mx-auto bg-gray-500/40 p-8">
    {
      terrenos?.map((terreno) => (
        <div class="bg-white rounded-2xl shadow-md overflow-hidden hover:shadow-xl transition-shadow duration-300">
          <img
            src={`https://ssrmztcxoijibjntrtqe.supabase.co/storage/v1/object/public/terrenos-imagenes/${terreno.src_imagen}`}
            alt={terreno.titulo}
            class="w-full h-48 object-cover p-3"
          />
          <div class="p-4 space-y-2">
            <h2 class="text-lg font-semibold text-gray-800">
              {terreno.titulo}
            </h2>
            <div>
                <p class="text-sm text-gray-600 pr-4-4">{terreno.descripcion}</p>
            </div>
            <p class="text-xs text-gray-500">📍 {terreno.ubicacion}</p>
            <p>{terreno.medidas} m2</p>
            <p class="text-green-700 font-bold text-sm">
              $ {terreno.precio?.toLocaleString()}
            </p>
          </div>
        </div>
      ))
    }
  </div>
</Layout>
